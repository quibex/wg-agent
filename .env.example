# WG-Agent Server Setup Configuration
# Only 3 variables are REQUIRED. Everything else has defaults.

# ============================================================
# REQUIRED: TLS Certificate Authority
# ============================================================
# Generate CA once: scripts/make-ca-only.sh
# Then encode: cat certs/ca.pem | base64 | tr -d '\n'

CA_CERT_PEM=your_base64_encoded_ca_certificate
CA_KEY_PEM=your_base64_encoded_ca_private_key

# ============================================================
# REQUIRED: Server Public IP
# ============================================================

SERVER_PUBLIC_IP=your.server.ip.address

# ============================================================
# OPTIONAL: Override defaults if needed
# ============================================================

# WG_AGENT_INTERFACE=wg0
# WG_AGENT_ADDR=0.0.0.0:7443
# WG_AGENT_HTTP_ADDR=0.0.0.0:8080
# WG_AGENT_RATE_LIMIT=10
# WG_SERVER_PORT=51820
# WG_SERVER_IP=10.8.0.1/24
# GITHUB_TOKEN=your_github_token_for_private_repos
