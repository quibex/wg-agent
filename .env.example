# WG-Agent Server Configuration
# ============================================================
#
# Переменные окружения для настройки wg-agent сервера.
# Скопируйте этот файл в .env и заполните ОБЯЗАТЕЛЬНЫЕ параметры.

# ============================================================
# ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ
# ============================================================

# Публичный IP адрес или домен сервера для подключения клиентов
# Этот адрес будет использован в конфигурациях клиентов как Endpoint
# Примеры: "1.2.3.4", "vpn.example.com"
SERVER_PUBLIC_IP=

# TLS сертификаты для mTLS аутентификации
# Сначала сгенерируйте CA: ./scripts/make-ca-only.sh
# Затем закодируйте в base64: cat certs/ca.pem | base64 | tr -d '\n'
CA_CERT_PEM=
CA_KEY_PEM=

# ============================================================
# WIREGUARD НАСТРОЙКИ (опционально)
# ============================================================

# Имя WireGuard интерфейса (по умолчанию: wg0)
# WG_AGENT_INTERFACE=wg0

# Подсеть для выделения IP адресов клиентам (по умолчанию: 10.8.0.0/24)
# WG_SUBNET=10.8.0.0/24

# Порт WireGuard сервера (по умолчанию: 51820)
# WG_SERVER_PORT=51820

# ============================================================
# СЕТЕВЫЕ НАСТРОЙКИ (опционально)
# ============================================================

# Адрес gRPC сервера (по умолчанию: 0.0.0.0:7443)
# WG_AGENT_ADDR=0.0.0.0:7443

# Адрес HTTP сервера для health checks (по умолчанию: 0.0.0.0:8080)
# WG_AGENT_HTTP_ADDR=0.0.0.0:8080

# ============================================================
# TLS ПУТИ (опционально)
# ============================================================

# Пути к TLS сертификатам (по умолчанию: /etc/wg-agent/...)
# WG_AGENT_TLS_CERT=/etc/wg-agent/cert.pem
# WG_AGENT_TLS_PRIVATE=/etc/wg-agent/key.pem
# WG_AGENT_CA_BUNDLE=/etc/wg-agent/ca.pem

# ============================================================
# ЛИМИТЫ (опционально)
# ============================================================

# Rate limit - максимум запросов в секунду (по умолчанию: 10)
# WG_AGENT_RATE_LIMIT=10
